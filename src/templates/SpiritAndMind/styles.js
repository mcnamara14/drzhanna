import styled from 'styled-components'
import { Row } from 'antd'

const Section = styled(Row)`
  margin-top: 60px;

  .gold-text, a {
    color: #d0a72d;
    font-weight: bold;
  }
`

export { Section }